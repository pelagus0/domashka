# 08_analytics_Amazon

SQL + Python мини‑задачи (воспроизводимо через Postgres в Docker).

## Запуск

```bash
docker compose up -d
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
python run.py
```

