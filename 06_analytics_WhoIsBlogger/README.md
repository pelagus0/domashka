# 06_analytics_WhoIsBlogger

Решение SQL‑задач на PostgreSQL с воспроизводимым запуском через Docker Compose.

## Запуск

```bash
docker compose up -d
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
python run.py
```

