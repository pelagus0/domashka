# 07_analytics_Cian

SQL‑задачи по схеме “библиотека” + текстовые ответы по метрикам объявлений и рекламе.

## Запуск SQL части

```bash
docker compose up -d
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
python run.py
```

